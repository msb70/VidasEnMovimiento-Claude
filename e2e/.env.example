# Copia a .env si quieres sobreescribir los valores por defecto.
# Por defecto, las pruebas corren contra el sitio en vivo con las cuentas demo.

BASE_URL=https://vidasenmovimiento.com

# Cuenta admin (demo). El login con estas credenciales debe funcionar.
E2E_ADMIN_EMAIL=afonseca@demo.com
E2E_ADMIN_PASSWORD=Demo2024!

# Cuenta operador de prueba (la crea el setup de /tests con su organización).
E2E_OPERATOR_EMAIL=test-operator@viamovimiento.test
E2E_OPERATOR_PASSWORD=Test-Operator-2026!
